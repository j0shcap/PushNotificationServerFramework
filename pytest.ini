[pytest]
pythonpath = .
testpaths = tests
markers =
    integration: boots the real server against real Postgres (needs INTEGRATION_DB_HOST)
    apns_sandbox: calls Apple's real APNs sandbox (opt in with APNS_SANDBOX_E2E=1)
